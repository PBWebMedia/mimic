image: $CI_REGISTRY/backend/docker/images/php:7.4-deploy

stages:
  - build
  - test
  - deploy

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - vendor/

variables:
  DOCKER_DRIVER: overlay2

build:
  stage: build
  script:
    - composer install -n --prefer-dist -o --no-ansi --no-progress
  artifacts:
    expire_in: 30 min
    paths:
      - vendor/
      - app/config/parameters.yml

test:
  image: $CI_REGISTRY/backend/docker/images/php:7.4-pcov-cli
  stage: test
  dependencies:
    - build
  script:
    - SYMFONY_DEPRECATIONS_HELPER=disabled vendor/phpunit/phpunit/phpunit -d memory_limit=512M --coverage-text --coverage-html coverage
  artifacts:
    paths:
      - coverage/
  only:
    - master

pages:
  stage: deploy
  dependencies:
    - test
  script:
    - mv coverage public
  artifacts:
    expire_in: 30 days
    paths:
      - public/
  only:
    - master
  variables:
    GIT_STRATEGY: none
